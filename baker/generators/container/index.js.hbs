/*
 *
 * {{ properCase name }}
 *
 */

import React, { Component, View, Text } from 'react-native';
import { connect } from 'react-redux';
import styles from './styles';
{{#if wantSelector}}

import { createSelector } from 'reselect';
{{#if selectorName}}
import {{camelCase selectorName}}Selector from '../../selectors/{{camelCase selectorName}}Selector';
{{else}}
{{#each selectors }}
import {{this}} from '../../selectors/{{this}}';
{{/each}}
{{/if}}
{{/if}}


class {{ properCase name }} extends Component {
  render() {
    return (
      <View>
        <Text>{{ properCase name }}</Text>
      </View>
    );
  }
}

{{#if wantSelector}}
const mapStateToProps = createSelector(
{{#if selectorName}}
  {{camelCase selectorName}}Selector
{{else}}
{{#each selectors }}
  {{this}},
{{/each}}
{{/if}}
);
{{else}}
function mapStateToProps(state) {
  return {};
}
{{/if}}

function mapDispatchToProps(dispatch) {
  return {
    dispatch,
  };
}

export default connect(mapStateToProps, mapDispatchToProps)({{ properCase name }});
